package parser;

public enum TokenType {
    NUMBER,
    WORD,
    PLUS,
    MINUS,
    STAR,
    SLASH,
    EQUAL,
    PRINT,
    LBRACKET,
    RBRACKET,
    EOF,
    STRING,
    IF,
    ELSE,
    LT,
    GT
}
